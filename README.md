# Face-Detection-with-Haar-Cascades
This project implements face and eye detection using OpenCV Haar Cascades. It extracts an ROI, detects faces and eyes in static images, and performs real-time face detection with labels using a webcam. Haar Cascade XML classifiers are used for efficient object detection.
